[project]
sourceRoot   = "ai-golf-coach-mvp"
packageManager = "yarn"

[phases.setup]
nixPkgs = ["nodejs_20", "yarn"]

[phases.install]
command = "yarn install --frozen-lockfile"

[phases.build]
command = "yarn build"

[phases.start]
command = "yarn start"
